import { Box } from '@mui/material';
import { yellow } from '@mui/material/colors';

export const SideBar = () => {
	return (
		<Box sx={{ backgroundColor: yellow[300], height: '100%' }}>SideBar</Box>
	);
};
