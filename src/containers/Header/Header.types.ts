export type headerProps = {
	/**
	 * Action that will happen on clicking menu button on mobile and tablet screen
	 * @returns
	 */
	onMenuClick: () => void;
	/**
	 * State of sidebar weather to show it to user or not
	 */
	hideSideBar?: boolean;
};
